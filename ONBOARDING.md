# دليل سريع للمشتركين الجدد

## نظرة عامة على الهيكل الحالي

حاليًا هذا المستودع في حالة **تهيئة أولية جدًا**:

- لا توجد شيفرة مصدر فعلية بعد.
- لا توجد مجلدات تطبيق/خدمات (`src/`, `app/`, `backend/` ...).
- لا توجد إعدادات بناء أو تشغيل (`package.json`, `pyproject.toml`, `Dockerfile` ...).
- الملف المتتبع الوحيد هو `.gitkeep` للإبقاء على المستودع غير فارغ.

## ماذا يعني ذلك لفريق جديد؟

هذا يعني أن المشروع لم يبدأ عمليًا بعد، وأن أفضل ما يمكن فعله الآن هو وضع **هيكل أساسي واضح** قبل بدء التطوير، حتى تكون المساهمة سهلة ومنظمة.

## هيكل مقترح كبداية

يمكن اعتماد هيكل عام بسيط (قابل للتخصيص حسب التقنية):

```text
.
├─ README.md                # تعريف المشروع وطريقة التشغيل
├─ docs/
│  ├─ architecture.md       # وصف معماري للنظام
│  ├─ onboarding.md         # نسخة مفصلة لتهيئة الأعضاء الجدد
│  └─ adr/                  # Architectural Decision Records
├─ src/                     # الشيفرة الأساسية
├─ tests/                   # اختبارات الوحدة/التكامل
├─ scripts/                 # سكربتات دعم (تهيئة، تنسيق، تشغيل محلي)
├─ .editorconfig
├─ .gitignore
└─ CI config                # مثل GitHub Actions
```

## أمور مهمة يجب معرفتها من اليوم الأول

1. **اتفاقية تسمية الملفات والمجلدات** (snake_case أو kebab-case أو PascalCase).
2. **استراتيجية الفروع** (مثل `main` + فروع ميزات + PRs قصيرة).
3. **معايير الجودة**:
   - تنسيق تلقائي (formatter)
   - linting
   - حد أدنى للتغطية الاختبارية
4. **سياسة التوثيق**: أي ميزة جديدة تحتاج تحديث README أو docs.
5. **تعريف واضح لبيئة التشغيل المحلية**: خطوات setup موحدة للجميع.

## خطة تعلم مقترحة للمشتركين الجدد (بعد الإعداد)

1. قراءة `README.md` (عند إضافته) لفهم الهدف ونطاق المشروع.
2. قراءة `docs/architecture.md` لفهم الحدود بين المكونات.
3. تشغيل المشروع محليًا عبر سكربت واحد موحّد.
4. تنفيذ تذكرة صغيرة (bugfix بسيط) للتعرف على دورة العمل.
5. فتح PR مع اختبارات وتوثيق مختصر.

## أولويات تنفيذ عملية (لصاحب المستودع)

- إضافة README أساسي خلال يوم واحد.
- اختيار stack تقني واضح وتثبيت tooling.
- إنشاء أول خط CI للتأكد من أن المشروع يبقى قابلًا للبناء والاختبار.
- تعريف قالب PR وقالب Issues.
- كتابة "Definition of Done" مختصرة لكل مساهمة.
